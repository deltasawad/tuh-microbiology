# ระบบส่งใบรายงานผลทางอีเมล

ปุ่ม **ส่งผลทางเมล** ในหน้ารายงานผล เรียก `POST /api/notify/email`
ฝั่งเซิร์ฟเวอร์อ่านใบจากฐานข้อมูล ประกอบอีเมล HTML แล้วส่งผ่านผู้ให้บริการที่ตั้งค่าไว้

---

## ทำไมผู้รับต้องมาจากฐานข้อมูล ไม่ใช่จาก request

ถ้ารับที่อยู่ผู้รับจาก body ตรง ๆ endpoint นี้จะกลายเป็น open relay ทันที
ใครรู้ URL ก็ยิงส่งเมลในนามโรงพยาบาลได้

`api/notify/email.js` จึงรับมาแค่ `reportId` แล้วไปอ่าน `reports.recipient_email`
ของใบนั้นเอง การเปลี่ยนผู้รับต้องเขียนลงใบก่อนผ่านหน้าเว็บที่มีล็อกอินและ RLS คุมอยู่
(หน้า `report_view.html` ทำให้แล้ว — แก้อีเมลในกล่องยืนยันได้ ระบบบันทึกลงใบก่อนส่ง)

---

## ตัวแปรที่ต้องตั้งบน Vercel

Settings → Environment Variables → Production แล้ว **deploy ใหม่** ค่าถึงจะมีผล

| ตัวแปร | ค่า | หมายเหตุ |
|---|---|---|
| `SUPABASE_URL` | `https://tgctyouhzsyizlosrmqh.supabase.co` | ไม่ใช่ความลับ |
| `SUPABASE_ANON_KEY` | anon key ตัวเดียวกับที่หน้าเว็บใช้ | **ไม่ใช่** service role |
| `MAIL_FROM` | `TUH Microbiology <อีเมลผู้ส่ง>` | ต้องตรงกับผู้ส่งที่ยืนยันไว้ทุกตัวอักษร |
| `MAIL_PROVIDER` | `resend` หรือ `sendgrid` | บังคับเมื่อตั้งคีย์ไว้ทั้งสองเจ้า |
| `RESEND_API_KEY` | คีย์ที่ขึ้นต้น `re_` | ตั้งเฉพาะเจ้าที่ใช้ |
| `SENDGRID_API_KEY` | คีย์ที่ขึ้นต้น `SG.` | ตั้งเฉพาะเจ้าที่ใช้ |
| `MAIL_REPLY_TO` | อีเมลห้องแล็บ | ไม่บังคับ |
| `PUBLIC_BASE_URL` | โดเมนหลัก | ไม่บังคับ ปกติเดาจาก header ได้เอง |

ตั้งไม่ครบ ระบบจะตอบ 503 พร้อมบอกชื่อตัวแปรที่ขาด แล้วหน้าเว็บแสดงให้เห็นตรง ๆ

### สลับผู้ให้บริการ

แก้ `MAIL_PROVIDER` ค่าเดียวแล้ว deploy ใหม่ ไม่ต้องลบคีย์อีกเจ้าทิ้ง
ถ้ามีคีย์ทั้งสองแต่ไม่ระบุ `MAIL_PROVIDER` ระบบจะไม่เดาให้ แต่ตอบกลับให้ไปตั้งค่า

---

## สถานะปัจจุบัน (28 ส.ค. 2569)

ใช้ **SendGrid** · ผู้ส่ง `TUH Microbiology <mrcabu166@gmail.com>` (Single Sender Verification)

| ผู้ให้บริการ | ส่งหาใครได้ | เข้า Inbox |
|---|---|---|
| **SendGrid** (ใช้อยู่) | ทุกคน | อาจตกกล่อง Junk ฉบับแรก |
| Resend + `onboarding@resend.dev` | เฉพาะเจ้าของบัญชี Resend | เข้าแน่นอน |

**สาเหตุที่ตกกล่องขยะ** — ผู้ส่งเป็น `@gmail.com` แต่ส่งออกจากเซิร์ฟเวอร์ SendGrid
ไม่ใช่ของ Google ปลายทางจึงตรวจลายเซ็นไม่ผ่านแล้วคัดเป็นสแปม
สถานะใน SendGrid ยังขึ้น Delivered เพราะปลายทางรับไว้แล้วจริง แค่ไม่ได้อยู่ใน Inbox

### ข้อความแจ้งหน่วยงานตอน rollout

> ระบบจะส่งใบรายงานผลจาก **TUH Microbiology**
> ฉบับแรกอาจอยู่ในกล่อง Junk/Spam ให้กด **Report not spam** หรือ **ไม่ใช่สแปม**
> หนึ่งครั้ง ฉบับถัด ๆ ไปจะเข้า Inbox ตามปกติ

---

## งานที่ยังเหลือ — ยืนยันโดเมน

แก้ได้ทีเดียวทั้งสองปัญหา คือส่งหาใครก็ได้ **และ** เข้า Inbox

ทำที่ SendGrid → Settings → Sender Authentication → **Domain Authentication**
ระบบจะให้ CNAME มา 3 รายการ เอาไปเพิ่มใน DNS ของโดเมน
พอขึ้น Verified แล้วเปลี่ยน `MAIL_FROM` เป็นอีเมลของโดเมนนั้น

**ตรวจก่อนว่าโดเมนมีอยู่จริงในระบบ DNS สาธารณะ**

```powershell
Resolve-DnsName -Name "<โดเมน>" -Server 8.8.8.8
```

เคยกรอก `nallied.tu.ac.th` ไว้ซึ่งไม่มีอยู่จริง ยืนยันไม่ผ่านแน่นอนไม่ว่าจะรอนานแค่ไหน
โดเมนที่มีอยู่จริงและใช้ Google Workspace รับเมลอยู่แล้วคือ `allied.tu.ac.th`

---

## รูปแบบอีเมล

* หัวเรื่อง `[เลขที่เอกสาร] ผลการตรวจ... · หน่วยงานที่เก็บตัวอย่าง · เวลาที่ส่ง`
  ใส่เวลาระดับวินาทีเพื่อไม่ให้ Gmail มัดเธรดแล้วยุบเนื้อหาเหลือปุ่ม `...`
  (มีหัวจดหมาย `X-Entity-Ref-ID` ที่ไม่ซ้ำช่วยอีกชั้น)
* หน่วยงานในหัวเรื่องและในตารางใช้ **จุดที่ไปเก็บตัวอย่าง** ไม่ใช่หน่วยงานผู้ส่งตรวจ
  เพราะ AIR-01 กับ WTS-03 งานอาชีวอนามัย/IC เป็นผู้ส่งทุกใบ ถ้าใช้ผู้ส่งจะเหมือนกันหมด
* **AIR-01** แยกคอลัมน์ `Bacteria (CFU)` และ `Fungus (CFU)` — รายงานเป็นจำนวนโคโลนี
* **บริการอื่น** คอลัมน์เดียว `ผลการตรวจ` — `No growth` หรือ `Growth — <ชื่อเชื้อ>`
* ช่องที่ยังไม่ลงผลขึ้น `รอตรวจ` ไม่เติม `0` แทน
  เพราะ 0 CFU แปลว่าตรวจแล้วไม่พบ คนละความหมายกับยังไม่ได้ตรวจ
* เกณฑ์มาตรฐานยกมาจาก `report_view.html` คำต่อคำทั้ง 8 บริการ (`SERVICE_STANDARD`)
  เพราะรายการย่อยส่วนใหญ่ไม่ได้เก็บ `standard_criteria` ไว้ในฐานข้อมูล
* มีฉบับข้อความล้วนแนบไปด้วยเสมอ สำหรับโปรแกรมเมลที่ไม่แสดง HTML

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ | ทางแก้ |
|---|---|---|
| 503 พร้อมรายชื่อตัวแปร | ตั้ง env ไม่ครบ หรือยังไม่ deploy ใหม่ | ตั้งให้ครบแล้ว deploy |
| 422 ใบนี้ยังไม่มีอีเมลผู้รับ | `recipient_email` ว่าง | กรอกในกล่องยืนยันตอนกดส่ง |
| 403 ผู้ส่งไม่ตรงกับที่ยืนยันไว้ | `MAIL_FROM` ไม่ตรง Sender ที่ verify | แก้ให้ตรงทุกตัวอักษร |
| 403 โหมดทดสอบ (Resend) | ยังไม่ได้ยืนยันโดเมน | ส่งได้เฉพาะเจ้าของบัญชี หรือยืนยันโดเมน |
| ตั้งคีย์ทั้งสองเจ้าแล้วไม่เปลี่ยน | ไม่ได้ตั้ง `MAIL_PROVIDER` | ตั้งให้ชัดเจน |
| เมลไม่ถึงแต่ API ตอบ 200 | ตกกล่องขยะ | ดู SendGrid Activity Feed / ค้น `in:anywhere` |
